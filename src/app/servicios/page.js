import { metadata } from "../layout"

export default function Servicios() {
	metadata.title = "DG-servicios"

	function change(evento){
		evento.preventDefault()
		evento.target.value

	}

	return (
		<div>
			
		</div>
	)
}